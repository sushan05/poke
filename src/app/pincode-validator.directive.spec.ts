import { PincodeValidatorDirective } from './pincode-validator.directive';

describe('PincodeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PincodeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
